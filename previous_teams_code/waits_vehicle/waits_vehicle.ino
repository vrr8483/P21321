#include "src/MainLoop.h"
void setup() {
  // put your setup code here, to run once:
}

void loop() {
  //Calls the actual main loop so we don't need to use Arduino IDE
  //Should never leave this function
  MainLoop();
}
